[package]
name = "{{project_name}}"
version = "0.1.0"
edition = "2021"
description = "{{description}}"
authors = ["{{author}}"]

[dependencies]
clap = { version = "4.5", features = ["derive"] }
anyhow = "1.0"
